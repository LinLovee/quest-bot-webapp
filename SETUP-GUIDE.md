# 🎮 RuneQuestRPG Web App — Полная инструкция по включению

## 📋 Содержание
1. Подготовка
2. Установка зависимостей
3. Развертывание
4. Включение Web App в боте
5. Тестирование

---

## 1️⃣ ПОДГОТОВКА

### Требования:
- Python 3.8+
- pip
- Git
- Аккаунт Telegram (для бота)
- Хостинг (Render.com, Replit, Railway, или локальный сервер)

---

## 2️⃣ УСТАНОВКА ЗАВИСИМОСТЕЙ

### Установи зависимости:
```bash
pip install -r requirements.txt
```

---

## 3️⃣ РАЗВЕРТЫВАНИЕ

### Вариант A: Локальное тестирование

1. **Создай `.env` файл:**
```env
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
WEBAPP_URL=http://localhost:5000
PORT=5000
```

2. **Создай папку `templates` и положи туда `index.html`:**
```bash
mkdir templates
# Скопируй index.html в эту папку
```

3. **Запусти бота:**
```bash
python webapp_bot.py
```

Откроешь http://localhost:5000 в браузере

---

### Вариант B: Развертывание на Render.com (РЕКОМЕНДУЕТСЯ)

#### Шаг 1: Создай репозиторий на GitHub
```bash
git init
git add .
git commit -m "Initial commit"
git push origin main
```

#### Шаг 2: Регистрация на Render.com
1. Перейди на https://render.com
2. Зарегистрируйся
3. Свяжи свой GitHub аккаунт

#### Шаг 3: Создай новый Web Service
1. Клик на "New +" → "Web Service"
2. Выбери свой репозиторий
3. Настройки:
   - **Name**: `runequest-webapp`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn webapp_bot:app`
   - **Port**: 5000

#### Шаг 4: Добавь переменные окружения
В разделе "Environment":
- `BOT_TOKEN` = твой токен бота
- `WEBAPP_URL` = URL твоего приложения на Render
- `PORT` = 5000

#### Шаг 5: Deploy
Нажми "Create Web Service" и жди деплоя (2-5 минут)

---

## 4️⃣ ВКЛЮЧЕНИЕ WEB APP В БОТЕ

### Способ 1: Через BotFather (ЛЕГКИЙ)

1. Открой **@BotFather** в Telegram
2. Выбери `/mybots` → выбери твоего бота
3. Нажми "Bot Settings" → "Menu button"
4. Нажми "Edit menu button URL"
5. Введи URL: `https://твой-url.onrender.com`

---

## 5️⃣ ТЕСТИРОВАНИЕ

### Проверка локально:
```bash
# Терминал 1: Запусти бота
python webapp_bot.py

# Терминал 2: Проверь API
curl http://localhost:5000/api/classes
```

---

**Готово! 🎉 Твой Web App работает!**
